<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="BaseTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="theme">@style/ThemeOverlay.AppCompat.Light</item>
        <!--<item name="popupTheme">@style/Base.V21.Theme.AppCompat.Light.Dialog</item>-->
        <item name="android:alertDialogTheme">@style/AlertDialog</item>
    </style>

    <style name="BaseTheme.TranslucentKitKatStatusBar" parent="BaseTheme" />

    <style name="ActionBar_Pop_Theme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:actionMenuTextColor">@color/primary_text</item>
    </style>

    <style name="AppTheme.ActionBar" parent="ThemeOverlay.AppCompat.Light">
        <item name="actionMenuTextColor">@color/primary_text</item>
    </style>

    <style name="AlertDialog" parent="@android:style/Theme.Holo.Light.Dialog">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

</resources>